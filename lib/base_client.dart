import 'package:flutter/material.dart';

class FeelBetter extends StatelessWidget {
  const FeelBetter({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [Text('How do you feel ? ')],
      ),
    );
  }
}
